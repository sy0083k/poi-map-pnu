## 🚀 시작하기

1. 이 프로젝트를 다운로드합니다.
2. `.env.example` 파일을 복사하여 `.env` 파일을 생성합니다.
3. `.env` 파일의 `VWORLD_KEY` 부분에 본인의 브이월드 API 키를 입력합니다.
4. `pip install -r requirements.txt`로 필요한 라이브러리를 설치합니다.
5. `python main.py`를 실행합니다!